backend:
  name: github
  repo: VernL/p-vernon-portfolio

media_folder: static/assets

collections:
  - name: post
    label: Posts
    folder: src/pages
    create: true
    fields: 
      - {name: title, label: Title}
      - {name: date, label: Created At, widget: date}
      - {name: url, label: URL}
      - {name: thumbnail, label: Thumbnail}
      - {name: body, label: Post, widget: markdown }