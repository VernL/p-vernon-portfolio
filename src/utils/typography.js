import Typography from "typography";
import parnassusTheme from "typography-theme-parnassus";

console.log(parnassusTheme)

const typography = new Typography(parnassusTheme);

export default typography;